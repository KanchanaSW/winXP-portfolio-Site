# Shutdown sound

Place a licensed `shutdown.mp3` file in this folder for the "Turn Off Computer" easter egg.

The app will attempt to play `/sounds/shutdown.mp3` and fail silently if the file is missing.
